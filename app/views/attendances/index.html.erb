<h1>Attendances#index</h1>
<p>Find me in app/views/attendances/index.html.erb</p>

<%= search_form_for @q, url: wedding_attendances_path(params[:wedding_id]) do |f| %>
  <p>
    <%= f.label :email_cont %>
    <%= f.text_field :email_cont %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= render @attendances %>

<%=link_to "Invite a guest", new_user_invitation_path %>